package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette (Professional Polish Forest Green Theme)
val TealPrimary = Color(0xFF1B5E20)       // Forest Green 900
val TealPrimaryContainer = Color(0xFFE8F5E9) // Mint / Green 50
val TealDark = Color(0xFF114D16)          // Rich Dark Green

val GoldCap = Color(0xFFF59E0B)           // Orange Cap (Gold)
val PurpleCap = Color(0xFF7C3AED)         // Purple Cap (Wickets)

// Neutrals for Dark Mode
val DarkBg = Color(0xFF0F172A)            // Slate 900
val DarkSurface = Color(0xFF1E293B)       // Slate 800
val DarkBorder = Color(0xFF334155)        // Slate 700

// Neutrals for Light Mode
val LightBg = Color(0xFFF7F9FC)           // Soft Professional Light Gray-Blue
val LightSurface = Color(0xFFFFFFFF)
val LightBorder = Color(0xFFE2E8F0)       // Slate 200
